# Volley Game Central – Backend environment example
# PostgreSQL configuration
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=volley_game_central

# Telegram bot configuration
TELEGRAM_BOT_TOKEN=your_bot_token
TELEGRAM_GROUP_ID=your_group_id           # ID of the group chat where announcements are posted
TELEGRAM_TOPIC_ID=                        # (optional) topic/thread ID for forum groups
MINI_APP_URL=http://localhost:3001        # URL where the Mini App is served from

# Server configuration
PORT=3000
