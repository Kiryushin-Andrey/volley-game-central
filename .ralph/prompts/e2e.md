Ralph loop — E2E pass, Suite {suite}.

{workflow_block}
{refs_block}

Execute Suite {suite} from {e2e} (§6–§8). Screenshots: {screenshots_dir}/.
Minimal fixes on {branch} only. Run feedback loops if you change code; commit and push.

{completion_block}
